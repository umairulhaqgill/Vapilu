# VITE_TENANT_SERVICE_URL / VITE_ORCHESTRATOR_URL / VITE_CONNECTOR_GATEWAY_URL:
# leave unset unless a backend lives on a different host than this page -
# all three default to window.location.hostname at runtime (see api.ts /
# CallSession.ts / connectorApi.ts).
VITE_TENANT_SERVICE_TOKEN=choose_a_different_random_secret_string_heretenant
VITE_ORCHESTRATOR_TOKEN=tokenizertokenoooktoken
VITE_CONNECTOR_GATEWAY_TOKEN=choose_a_different_random_secret_string_hereconnector
