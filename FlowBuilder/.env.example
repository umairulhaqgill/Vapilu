# VITE_TENANT_SERVICE_URL / VITE_ORCHESTRATOR_URL: leave unset unless the
# backend lives on a different host than this page - both default to
# window.location.hostname at runtime (see api.ts / CallSession.ts).
VITE_TENANT_SERVICE_TOKEN=choose_a_different_random_secret_string_heretenant
VITE_ORCHESTRATOR_TOKEN=tokenizertokenoooktoken
